"""
Run this file to start the FastAPI server !!!!!!!
用嚟啟動FastAPI Server嘅！
"""
import uvicorn
from app.config import settings

if __name__ == "__main__":
    uvicorn.run(
        "app.main:app",
        host=settings.host,
        port=settings.port,
        reload=True,
        log_level="info"
    )

